
import LeaderboardClient from '@/components/organizer/leaderboard-client';

export default function OrganizersPage() {
  // Data will now be fetched from the layout context
  return <LeaderboardClient />;
}
