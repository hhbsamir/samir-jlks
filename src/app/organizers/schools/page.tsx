
import SchoolsClient from '@/components/organizer/schools-client';

export default function SchoolsPage() {
  // Data will be provided by the context in the layout.
  // The client component will consume the context.
  return <SchoolsClient />;
}
